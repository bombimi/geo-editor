import { __name__(pascalCase) } from "./__name__(pascalCase)";

export * from "./__name__(pascalCase)";

declare global {
    interface HTMLElementTagNameMap {
        "ds-__name__(kebabCase)": __name__(pascalCase);
    }
}
